services:
  db:
    image: postgres:16
    container_name: gamedash_db
    environment:
      POSTGRES_DB: gamedash
      POSTGRES_USER: gamedash
      POSTGRES_PASSWORD: gamedash
    ports:
      - '5433:5432'
    volumes:
      - gamedash_pgdata:/var/lib/postgresql/data

volumes:
  gamedash_pgdata:
